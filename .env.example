# Used as the default API key in the in-app settings when local storage has no saved value.
DEFAUT_API_KEY=""

# Comma-separated default model ids shown in the in-app model selector.
# The first one is used as the default selected model.
DEFAULT_AI_MODELS="openai/gpt-4o-mini,anthropic/claude-3.5-haiku"
